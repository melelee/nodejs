// window.alert('s')
// let availHeight = screen.availHeight;
// console.log(availHeight)
let elementById = document.getElementById('1');
let value = elementById.value;
console.log(value)

let xmlHttpRequest = new XMLHttpRequest();
xmlHttpRequest.onreadystatechange = function () {
    if (xmlHttpRequest.readyState === 1) {

    }
    if (xmlHttpRequest.status === 1) {

    }
}

let element = document.createElement('script');

element.src = '1';
let elements = document.getElementsByTagName('head');
let htmlHeadElement = elements.item(0);
htmlHeadElement.appendChild(elements)
const array = [1];


// jsonp
// 把js作为返回的响应